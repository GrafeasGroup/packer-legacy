---
exclude_paths:
  - 'roles/thelonelyghost.python/tests/'
  - 'roles/grafeas.os_base/tests/'

skip_list:
  - '106'  # Ansible-Lint doesn't like Galaxy's namespacing convention: {username}.{role_name}

# vim: ft=yaml
